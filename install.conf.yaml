- defaults:
    link:
      relink: true

- clean: [
  '~'
]

- link:
    ~/.zshrc: 
      path: zsh/.zshrc    
      force: true
    ~/.config/kitty: 
      path: kitty
    ~/.config/nvim: 
      path: nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]  
